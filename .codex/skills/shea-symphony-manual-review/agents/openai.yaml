display_name: Shea Symphony Manual Review
short_description: Run an evidence-backed manual review pass for 3D-RAMS.
default_prompt: Review one 3D-RAMS issue or PR independently; run Shea CLI from the 3D-RAMS root by default for review-lane readiness/routing, then use gh, diff inspection, local checks, and a findings-first review handoff for focused fallback.
