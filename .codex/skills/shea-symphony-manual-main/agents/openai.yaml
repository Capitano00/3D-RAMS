display_name: Shea Symphony Manual Main
short_description: Run a scoped 3D-RAMS implementation session and hand off to review.
default_prompt: Work one 3D-RAMS implementation task from issue or prompt through scoped changes and verification; run Shea CLI main loop from the 3D-RAMS root by default, then use git, gh, local checks, and a clear review handoff for focused fallback.
